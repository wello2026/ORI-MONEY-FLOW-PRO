# خطة توسعة نظام ORI MINI ERP (المرحلة الرابعة: Enterprise Expansion)

بناءً على طلبك، سنقوم بنقل النظام من مجرد نظام مالي إلى نظام ERP متكامل وشامل (Enterprise Resource Planning). التوسعات المقترحة ستعالج مشاكل تعدد العملات وتضيف دورة المشتريات والموردين مع لمسات احترافية متقدمة.

## 1. تعدد العملات المتقدم (Multi-Currency System)
سيدعم النظام العملات المتعددة ليس فقط كشكل، بل كقيمة محاسبية حقيقية:
- **تحديث التحويلات:** إضافة حقل `exchange_rate` (معامل التحويل) وحقل `destination_amount` لجدول التحويلات.
- مثال: خصم `10,000` دينار ليبي من خزينة ليبيا، وإضافة `64,000` جنيه مصري لخزينة مصر، مع تسجيل معامل التحويل `6.4`.
- **فروق العملة (Forex Gain/Loss):** معالجة فروق العملة محاسبياً للحفاظ على توازن ميزان المراجعة.

## 2. إدارة جهات التعامل (Contacts: Suppliers & Customers)
بدلاً من مجرد تسجيل المعاملة باسم، سنقوم ببناء نظام متكامل لجهات التعامل:
- **جدول `contacts`:** لتسجيل بيانات الموردين، العملاء، والموظفين.
- **الحسابات الجارية (Current Accounts):** كل مورد أو عميل سيكون له حساب مالي مرتبط به تلقائياً (Accounts Payable / Accounts Receivable).
- **كشف حساب جهة:** إمكانية إصدار كشف حساب مفصل لكل مورد يوضح الدفعات والفواتير والرصيد المتبقي.

## 3. دورة المشتريات والمصروفات المتقدمة (Procurement)
- **فواتير المشتريات (Purchase Bills):** تسجيل فواتير المشتريات من الموردين وإنشاء قيد استحقاق آلي.
- **سندات الصرف والقبض:** ربط الدفعات بالفواتير مباشرة (مثلاً: سداد جزء من فاتورة مورد).

## 4. إضافات احترافية مقترحة (Enterprise Features)
لجعل النظام إبداعياً وتنافسياً، أقترح إضافة:
- **مراكز التكلفة (Cost Centers):** إذا كان لديك أكثر من فرع أو مشروع، يمكنك توجيه الإيرادات والمصروفات لمركز تكلفة محدد لمعرفة أرباح كل فرع/مشروع على حدة.
- **المخزون المبسط (Simple Inventory):** إذا كانت المشتريات عبارة عن بضاعة، يمكننا تتبع الكميات المتوفرة (اختياري).
- **محدث أسعار العملات الآلي (Live Exchange Rates):** ربط النظام بـ API يجلب أسعار الصرف العالمية يومياً كقيمة مقترحة (يمكن للمستخدم تعديلها).

## 5. نظام الإشعارات الفوري (Real-time Notification System)
تم تنفيذ الإصلاحات التالية:
- **محرك الإشعارات (DB Triggers):** تمت أتمتة الإشعارات للمديرين عند التحويلات.
- **إصلاح الصوت في iOS:** إضافة آلية "Audio Unlock" لتجاوز قيود Safari.

## 6. إشعارات الخلفية (Background Push Notifications) - قيد التنفيذ
لتصل الإشعارات حتى والتطبيق مغلق، سنقوم بتفعيل تقنية Web Push:
- **نظام التشفير (VAPID):** استخدام مفاتيح VAPID لتأمين الاتصال مع Apple/Google.
- **جدول الاشتراكات:** إنشاء جدول `push_subscriptions` لتخزين معرفات أجهزة المستخدمين.
- **Service Worker:** تحديث كود الخلفية لاستقبال الرسائل المنبثقة وعرضها.
- **Supabase Edge Function:** إنشاء وظيفة برمجية على السيرفر تقوم بإرسال الإشعار فوراً عبر نظام Apple Push Notification service (APNs).

---

> [!IMPORTANT]
> **User Review Required**
> يرجى مراجعة الخطة أعلاه. 
> 
> **أسئلة للنقاش:**
> 1. هل ترغب في إضافة وحدة **إدارة المخزون** والأصناف؟
> 2. هل نعتمد "الدينار الليبي" كعملة الأساس؟
> 3. بالنسبة لإشعارات الخلفية، ستحتاج للموافقة على طلب "إرسال التنبيهات" الذي سيظهر في المتصفح.

## Proposed Changes (الهيكلية التقنية)

### Database Migrations
#### [NEW] `010_push_subscriptions.sql`
- إنشاء جدول `push_subscriptions` (user_id, subscription_json, device_type).

### Service Worker & PWA
#### [NEW] `src/sw.ts`
- كود الخلفية لمعالجة أحداث `push` و `notificationclick`.
#### [MODIFY] `vite.config.ts`
- تحديث إعدادات PWA لاستخدام Service Worker مخصص يدعم الإشعارات.

### Frontend
#### [NEW] `src/components/notifications/PushManager.tsx`
- مكون لإدارة طلب صلاحية الإشعارات والاشتراك في الخدمة.

### Server (Supabase Edge Functions)
#### [NEW] `supabase/functions/send-push/index.ts`
- وظيفة برمجية لإرسال الإشعارات الحقيقية عبر بروتوكول Web Push.

### Store Management
#### [MODIFY] `src/stores/transferStore.ts`
- دعم مدخلات العملة المتعددة وإرسال القيم المحولة.
#### [NEW] `src/stores/contactStore.ts`
- لإدارة الموردين والعملاء ودعم الـ Offline-First.

### User Interface
#### [MODIFY] `src/components/ui/ToastNotifications.tsx`
- تحسين تأثيرات الحركة (Animations) وإضافة أصوات تنبيهية اختيارية.
#### [NEW] `src/pages/contacts/ContactsPage.tsx`
- شاشة إدارة الموردين والعملاء.
#### [MODIFY] `src/pages/transfers/TransferFormPage.tsx`
- تحديث الشاشة لدعم "سعر الصرف" وحساب المبلغ النهائي آلياً.

## Verification Plan
- **اختبار الإشعارات:** القيام بعملية تحويل من حساب موظف ومراقبة وصول الإشعار فوراً لحساب المدير على جهاز آخر.
- **تعدد العملات:** محاكاة تحويل مالي بين حسابين بعملتين مختلفتين والتأكد من صحة الرصيدين.
- **إدارة الموردين:** إنشاء مورد جديد، تسجيل فاتورة مستحقة، ثم تسجيل دفعة له، والتحقق من كشف حسابه.
